// function expressions
//
// a js function can also be defined using an expressions
//
// a function expression can be stored in a variable
//

const x = function (a, b) {return a * b};
let z = x(4, 3);
console.log(z);

// the function above is actually  an anonymous function(a function without a name)
// functions stored in variables do not need function names
// they are invoked usign the variable name
//
// the function end wwith a semicolon because it is a part of an executable statement
